/** Set after user accepts legal declaration during worker signup; cleared on registration success. */
export const REG_DECLARATION_STORAGE_KEY = 'gigease_reg_declaration_ok';

/** Insurer name as used in proposal / schedule copy (replace for production branding). */
export const REG_DECLARER_INSURER_NAME = 'XYZ Insurance Co.';
